{% extends 'base.html.twig' %}

{% block title %}S'enregistrer{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>S'enregistrer</h1>

        {{ form_errors(registrationForm) }}

        {{ form_start(registrationForm, {'attr': {'class': 'form'}}) }}
            <div class="form-group">
                {{ form_row(registrationForm.username, {'label': 'Pseudo', 'attr': {'class': 'form-control mb-3', 'placeholder': 'Pseudo'}}) }}
            </div>
            <div class="form-group">
                {{ form_row(registrationForm.plainPassword, {
                    'label': 'Mot de passe',
                    'attr': {'class': 'form-control mb-3', 'placeholder': 'Mot de passe'}
                }) }}
            </div>
            <div class="form-check">
                {{ form_row(registrationForm.agreeTerms, {'label': 'J\'accepte', 'attr': {'class': 'form-check-input mb-3'}}) }}
                {{ form_label(registrationForm.agreeTerms, 'de vendre mon âme', {'label_attr': {'class': 'form-check-label mb-3'}}) }}
            </div>

            <button type="submit" class="btn btn-primary mb-3">S'enregistrer</button>
        {{ form_end(registrationForm) }}
    </div>
{% endblock %}

